<Window
    x:Class="GestionSuiviFacture.WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:comp="clr-namespace:GestionSuiviFacture.WPF.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:GestionSuiviFacture.WPF.ViewModels"
    Title="Gestion Suivi Facture"
    Width="1200"
    Height="700"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <!--  Add drop shadow effect  -->
    <Border Background="Transparent" CornerRadius="15">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="25"
                Opacity="0.6"
                ShadowDepth="0"
                Color="#20000000" />
        </Border.Effect>

        <Border
            Background="White"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            CornerRadius="15">
            <comp:PageLayout DataContext="{Binding}" />
        </Border>
    </Border>
</Window>