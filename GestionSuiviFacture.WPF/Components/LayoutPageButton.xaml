<UserControl x:Class="GestionSuiviFacture.WPF.Components.LayoutPageButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestionSuiviFacture.WPF.Components"
             mc:Ignorable="d" 
             x:Name="root">
    <UserControl.ContentTemplate>
        <DataTemplate>
            <Grid Margin="5 10 5 10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Image Grid.Row="0"  Source="{Binding Image, ElementName=root}"/>
                <TextBlock VerticalAlignment="Center" Text="{Binding Header, ElementName=root}" 
                           HorizontalAlignment="Center" Width="48" TextWrapping="Wrap" Foreground="White" 
                           TextAlignment="Center" Grid.Row="1" FontWeight="SemiBold" FontSize="10"/>
            </Grid>
        </DataTemplate>
    </UserControl.ContentTemplate>
</UserControl>