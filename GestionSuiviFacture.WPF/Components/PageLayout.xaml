<UserControl x:Class="GestionSuiviFacture.WPF.Components.PageLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestionSuiviFacture.WPF.Components"
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="580" d:DesignWidth="960">
    
    <UserControl.ContentTemplate>
        <DataTemplate>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                
                <Grid Grid.Column="0" Background="#353535" MouseLeftButtonDown="DragBar_MouseLeftButtonDown" Grid.IsSharedSizeScope="True">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    
                    <Image Grid.Row="0" Source="pack://application:,,,/Images/marjane.png" Margin="0 20 0 50" Width="60" Height="65"/>

                    <UniformGrid Grid.Row="1" Columns="1" Rows="3">
                        <local:LayoutPageButton Image="pack://application:,,,/Images/facture.png" Header="F.Normal"/>
                        <local:LayoutPageButton Image="pack://application:,,,/Images/factureEmba.png" Header="F.Emballage"/>
                        <local:LayoutPageButton Image="pack://application:,,,/Images/search.png" Header="Consultation"/>
                    </UniformGrid>
                </Grid>
                
                <ContentPresenter Grid.Column="1" Content="{TemplateBinding Content}"/>
            </Grid>
        </DataTemplate>
    </UserControl.ContentTemplate>
</UserControl>